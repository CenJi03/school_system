from django.urls import path
from . import views

# Define urlpatterns as a list - this was likely missing or incorrectly defined
urlpatterns = [
    # Add your URL patterns here, for example:
    # path('list/', views.quality_list, name='quality_list'),
    # path('detail/<int:pk>/', views.quality_detail, name='quality_detail'),
]
